# 1:1 Review — CarroQueSí — 2026-07-08

## Overview
Fifth review. Since June 22: Playwright e2e initial setup shipped (#92, #93), which introduced a real regression — an App Check dev/prod branching bug that blocked receipt scanning — caught by the user within minutes and fixed same session (#94). A substantial list action sheet refactor landed (#95), and TODO.md moved to Linear (#96) as the new source of truth. The project has been quiet for a week due to vacation. Main theme: the specific failure mode from past reviews (shipping cross-cutting changes without a targeted "what else could this break" pass) has resurfaced in a new shape — environment/config branching instead of mobile UI.

---

## AI: Self-Assessment

**Wins**
- Playwright e2e initial setup is a clean, scoped first step — full rollout (visual regression) correctly identified as still pending and still gated behind a `/brainstorming` session, rather than sliding into scope creep
- List action sheet refactor (#95) fixed real state bugs (rename/delete not updating local state, Escape key conflicts) alongside the structural cleanup
- TODO.md → Linear migration removes a document that reliably went stale next to the actual source of truth

**Areas for Improvement**
- The App Check regression (#94): splitting an `if/else` guard into two separate `if` blocks during the e2e refactor silently changed dev-vs-prod initialization behavior. This is exactly the class of bug `verification-before-completion` exists to catch — tests and lint passed, but the actual environment-conditional behavior wasn't diffed before calling it done. The user caught it by hand (403 in the console), not me.
- This is the third review running where a shipped change slipped a defect through afterward — mobile input bugs (#87, #88) in June, now an environment-config regression in #94. The surface keeps moving (mobile QA → environment logic) but the root cause is the same: cross-cutting refactors aren't getting an explicit "what does this change besides the obvious diff" pass.
- The mobile QA checklist commitment from June 22 is still unverified for a second review in a row — no UI-only bug fixes shipped in this window to test it against.

**Commitments**
- For any refactor touching environment-conditional logic (dev/prod branches, feature flags, config gating), explicitly verify both branches' behavior before calling the change done — not just running the test suite.
- Carry forward: end agent-driven UI sessions with an explicit on-device QA note (still unverified, no test case yet).

---

## Your Workflow: Observations & Recommendations

**What's Working**
- Scoping the e2e rollout correctly — landing the initial Playwright setup without forcing the full visual-regression brainstorming step onto a step that didn't need it yet
- Catching the App Check regression fast (403 in console, obvious cause) kept the fix cost low even though it shouldn't have shipped in the first place

**Opportunities**
- `CHANGELOG.md`'s `[Unreleased]` section has sat since June 24 with no release cut — the pre-push hook only blocks on staleness, not on how long something sits unreleased. Worth deciding on a release cadence (e.g., weekly, or "cut whenever Unreleased is non-empty for >X days") rather than relying on remembering. You've already committed to cutting one right after this session, which resolves the immediate case.
- Now that Linear is the system of record for tasks, it's worth telling me up front (next time it's relevant) whether there's a convention for priority/labels I should trust when reading it — otherwise I'll default to reading it the same ad hoc way I read TODO.md.

---

## Action Items

| Owner | Item | Priority |
|-------|------|----------|
| AI | For refactors touching environment-conditional logic, explicitly verify both branches' behavior before calling the change done | High |
| You | Cut the pending release for the App Check fix (#94) | High |
| AI | Mobile QA checklist commitment — still unverified, carry forward | Medium |
| You | Resume agent queue / autonomous loop research on return from vacation | Medium |
| You | Run `/brainstorming` before the full Playwright e2e + visual regression rollout (initial setup done, rollout still pending) | Medium |
| You | Document AI vs. Gemini comparison for receipt scanning — still parked | Low |

---

## Next Review
Suggested: 2026-07-15, or your first working day back from vacation — whichever is later.
