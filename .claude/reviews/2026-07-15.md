# 1:1 Review — CarroQueSí — 2026-07-15

## Overview
Sixth review. Since 07-08: the full Playwright e2e + visual regression rollout shipped as a properly sequenced 4-PR chain (design spec #102, infra #103, purchase-lifecycle coverage #106, receipt-scanning coverage #107), a Docker baseline-generation fix needed a same-day follow-up (#104 → #105), the barcode scanner dark-mode fix landed with real visual verification (#99), and a receipt-matching bug got a properly iterated fix (#108). Release 0.19.3 is prepped and merged. Main theme: the three-review recurring pattern (ship → defect discovered after close) did **not** repeat — the one candidate case traced to a tooling timing issue, not a missed verification pass — and one long-standing commitment gets recalibrated to match the project's actual stage instead of carried forward as an unresolved miss.

---

## AI: Self-Assessment

**Wins**
- The e2e + visual regression rollout followed the process committed to on 06-22: brainstorming spec first (#102), then scoped implementation PRs — no scope creep, no skipped design step
- Receipt-matching fix (#108) iterated to an actual root cause within one PR (date-window filter → dedup → closest-purchase preference) instead of shipping a shallow first pass
- PR #99's test plan included real visual verification (DevTools `prefers-color-scheme` emulation, light and dark, explicit no-regression check) — the first concrete instance of applying the verification-before-completion habit to a UI fix

**Areas for Improvement**
- #104 → #105 initially looked like a repeat of the three-review pattern (ship, defect surfaces after). It wasn't — #105 fixed something Gemini's review flagged after #104 had already merged, a tooling-timing gap, not a missed check on my part. Correctly identified this only by asking rather than assuming; worth staying skeptical of surface-level pattern matches going forward.
- The environment-conditional-logic verification commitment (from #94) still has no clean test case to prove out — nothing shipped this session that actually exercises dev/prod branching. Carrying forward rather than closing.

**Commitments**
- Keep verifying environment-conditional logic explicitly the next time a change touches it — still unproven against a real case.
- Retire the "on-device QA checklist" framing (see recalibration below) and replace it with a bar suited to the project's current stage.

---

## Your Workflow: Observations & Recommendations

**What's Working**
- Brainstorming-before-build discipline is holding for a second review running — the e2e rollout is the second consecutive case of a committed `/brainstorming` step actually happening before implementation
- Calibrated trust in Gemini's automated review — neither blind trust nor dismissal; correctly read this incident as "Gemini is inconsistent," not "the process failed"
- Consistent right-sizing of effort to project stage: on-device QA and the Docker emulation slowdown were both explicitly deferred as "not worth it yet," which matches the same reasoning already applied to infra-risk deferral in earlier reviews

**Opportunities**
- **Recalibration, not a miss:** the on-device QA commitment (open since 06-22) is confirmed as aspirational for now — too early for that level of polish at current scale. Closing it out rather than carrying it forward as unresolved; DevTools-level visual verification is the working bar until that changes.
- **Docker-isolated baseline generation is a new infra dependency** (#104) — AGENTS.md specifically calls out new infra dependencies as ADR-worthy. Worth a quick pass to decide if it deserves an ADR entry alongside the existing five.
- **Two items have now been parked for three consecutive reviews** (agent queue/loop research, MCP/Siri brainstorming — both since 06-22). Worth an explicit call: still active goals worth tracking, or drop them so the open-items list doesn't accumulate stale clutter.

---

## Action Items

| Owner | Item | Priority |
|-------|------|----------|
| AI | For refactors touching environment-conditional logic, explicitly verify both branches' behavior before calling the change done | Medium |
| AI | Flag mobile-path issues (input type, viewport, safe area) during implementation, not just at handoff | Medium |
| AI | Check whether Docker-isolated visual regression baseline generation warrants an ADR entry | Low |
| You | Decide: are agent queue research and MCP/Siri brainstorming still active goals (parked 3 reviews running), or should they come off the tracked list? | Medium |
| You | Bring a Document AI vs. Gemini comparison into the next receipt scanning `/brainstorming` session | Low |

---

## Next Review
Suggested: 2026-07-22
