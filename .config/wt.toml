# Worktrunk project config — shared by all contributors
# See: https://worktrunk.dev

[pre-start]
# See: .envrc
direnv = "direnv allow"

[[post-start]]
# See: .worktreeinclude
copy = "wt step copy-ignored"

[[post-start]]
deps = "just install"

[[post-start]]
migrate = "just backend migrate"

[[post-start]]
seed = "just backend seed"

[[pre-commit]]
lint = "just lint"

[[pre-merge]]
ci = "just ci"
build = "just frontend build"

