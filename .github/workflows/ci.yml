name: CI

on:
  pull_request:

# NOTE: deliberately NO filter of any kind on this trigger — no `paths:`, and no
# `branches:` either.
#
# A workflow skipped by a filter never posts its checks at all, so any required
# context it owns sits at "Expected — waiting for status" forever and the PR can
# never merge. A JOB skipped by an `if:` condition posts SUCCESS. That asymmetry
# is why the gating lives on the jobs, not on the trigger.
# https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/collaborating-on-repositories-with-code-quality-features/troubleshooting-required-status-checks
#
# `branches:` is the same trap wearing a different hat, and it bites in a way
# that is quieter than the `paths:` one. For `pull_request` it filters on the
# BASE branch, so `branches: [main]` silently switched CI off for every PR based
# on anything else — which is exactly what a stack of phase PRs into a long-lived
# integration branch is. The PR page showed zero checks: not red, not pending,
# and `mergeStateStatus: CLEAN` because no ruleset applies to that base. Green,
# and nothing had run. Found on #163 after it was retargeted off `main`.
#
# Removing the filter costs a little CI time on stacked PRs and buys back the
# thing the stack most needs: each phase verified as it lands, rather than five
# of them fused together on the release PR with nothing to bisect.

# Supersede in-flight runs for the same PR. Before this, a single branch could
# rack up ten runs where only the last one mattered.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    name: Detect changed areas
    runs-on: ubuntu-latest
    outputs:
      frontend: ${{ steps.filter.outputs.frontend }}
      backend: ${{ steps.filter.outputs.backend }}
      tooling: ${{ steps.filter.outputs.tooling }}

    # Only what `gh pr diff` needs. Stated explicitly so the job does not
    # inherit — or silently lose — its capability from a repository-wide
    # default that someone can flip in Settings.
    permissions:
      contents: read
      pull-requests: read

    steps:
      - uses: actions/checkout@v7

      - id: filter
        env:
          GH_TOKEN: ${{ github.token }}
          PR: ${{ github.event.pull_request.number }}
          REPO: ${{ github.repository }}
        run: |
          # `set +e` so a failure here cannot skip the jobs below: a skipped
          # job reports SUCCESS to a required check, so failing closed would
          # wave the PR through with nothing verified. `gh` returning nothing
          # reaches the classifier as empty stdin, which it treats as
          # "run everything". `CI gate` catches this case too.
          set +e
          files=$(gh pr diff "$PR" --repo "$REPO" --name-only)
          printf '%s\n' "$files" | scripts/ci-changed-areas.sh | tee -a "$GITHUB_OUTPUT"

  frontend:
    name: Frontend (typecheck + lint + format + test)
    needs: changes
    if: needs.changes.outputs.frontend == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6
        with:
          package_json_file: frontend/package.json

      - uses: actions/setup-node@v7
        with:
          node-version-file: frontend/.nvmrc
          cache: pnpm
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm typecheck

      - name: Lint
        run: pnpm lint

      - name: Format check
        run: pnpm format:check

      - name: Test
        run: pnpm test

  backend:
    name: Backend (lint + format + tests)
    needs: changes
    if: needs.changes.outputs.backend == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v7

      - uses: astral-sh/setup-uv@v8.3.2
        with:
          python-version: "3.13"
          enable-cache: true

      - name: Install dependencies
        run: uv sync

      - name: Lint
        run: uv run ruff check

      - name: Format check
        run: uv run ruff format --check

      - name: Test
        run: uv run pytest

  tooling:
    name: Repo tooling (guardrail hooks + CI filter)
    needs: changes
    if: needs.changes.outputs.tooling == 'true'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      # Deliberately no uv/pnpm setup: these guard the agent's own tooling and
      # the CI filter itself, and must run on a bare checkout with only system
      # python3. Do not add a dependency to either test — needing one is a sign
      # the thing under test belongs in a script, not embedded somewhere that
      # requires a parser to reach.
      - name: Test guardrail hooks
        run: python3 .claude/hooks/test_hooks.py

      - name: Test CI changed-area filter
        run: python3 scripts/test-ci-changed-areas.py

  # E2E. Shares the `changes` classifier with everything else rather than
  # carrying its own `paths:` list — two encodings of one rule drift, and the
  # previous pair already had (SHARED covered all of .github/workflows/, the
  # paths list only covered playwright.yml).
  #
  # Gated on `frontend` because these specs never touch a real backend: no
  # FastAPI process or Postgres runs here, and tests/fixtures.ts mocks every
  # backend route from tests/fixtures.json. A Python change cannot fail them.
  # What covers that gap is a backend contract test, which validates those
  # fixtures against the response model each route really declares — so a
  # response-shape change fails the backend job. That is also why the backend
  # area matches fixtures.json as well as backend/.
  playwright:
    name: Playwright Tests
    needs: changes
    if: needs.changes.outputs.frontend == 'true'
    timeout-minutes: 60
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6
        with:
          package_json_file: frontend/package.json

      - uses: actions/setup-node@v7
        with:
          node-version-file: frontend/.nvmrc
          cache: pnpm
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Playwright Browsers
        run: pnpm exec playwright install --with-deps

      - name: Run Playwright tests
        run: pnpm exec playwright test

      - uses: actions/upload-artifact@v7
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: frontend/playwright-report/
          retention-days: 30

  # The single required status check. Every job above is free to be renamed,
  # split, or skipped without touching the repository ruleset.
  #
  # `if: always()` is what makes this correct rather than decorative — without
  # it, an upstream failure would skip this job, and a skipped required check
  # reports SUCCESS. With it, `changes` blowing up fails the gate instead of
  # silently waving the PR through.
  #
  # `playwright` is deliberately ABSENT from `needs`. E2E is advisory today —
  # a PR can merge with it red. Adding it here makes it blocking, which is a
  # real policy change and should be made on purpose, not inherited from the
  # refactor that moved it into this file. The flakiness that was being
  # assessed had one cause, an empty offline-queue drain that re-read the list
  # and overwrote a write the test had just made; it is fixed, and the suite
  # then ran clean on first attempt across sixteen runs with retries off. That
  # is a reason to consider promoting this, not a promotion.
  ci-gate:
    name: CI gate
    needs: [changes, frontend, backend, tooling]
    if: always()
    runs-on: ubuntu-latest

    steps:
      - name: Verify no upstream job failed
        run: |
          set -uo pipefail
          results='${{ join(needs.*.result, ' ') }}'
          echo "Upstream results: $results"
          for r in $results; do
            case "$r" in
              success|skipped) ;;
              *) echo "::error::An upstream CI job reported '$r'"; exit 1 ;;
            esac
          done
          echo "All upstream jobs succeeded or were skipped."
