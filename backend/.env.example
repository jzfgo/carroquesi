# ALLOWED_ORIGINS=["http://localhost:5173","https://your-app.web.app"]
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/carroquesi
# FIREBASE_CREDENTIALS_PATH=firebase-credentials.json
# DEV_AUTH_BYPASS=true  # local dev only — skips Firebase token validation when X-Dev-User-Id header is present
# RECEIPT_STORAGE_BUCKET=your-project-id.appspot.com  # GCS bucket for receipt images
# GCP_PROJECT=your-gcp-project-id  # required locally when using ADC (gcloud auth application-default login)
