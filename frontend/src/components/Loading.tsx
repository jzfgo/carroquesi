import './Loading.css'

export function Loading() {
  return (
    <div
      role="status"
      aria-label="Cargando"
      className="loading"
      style={{

      }}
    >
      <span
        className="loading__spinner"
        style={{

        }}
      />
    </div>
  )
}
